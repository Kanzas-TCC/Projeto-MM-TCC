import { Request, Response } from "express";

export const postAds = async (req: Request, res: Response) => {
    try {
        // Lógica para criar um anúncio
    } catch (error) {
        console.error(error);
    }
}

export const editAds = async (req: Request, res: Response) => {
    try {
        // Lógica para editar um anúncio
    } catch (error) {
        console.error(error);
    }
}

export const deleteAds = async (req: Request, res: Response) => {
    try {
        // Lógica para deletar um anúncio
    } catch (error) {
        console.error(error);
    }
}
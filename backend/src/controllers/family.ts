import { Request, Response } from "express";

export const postFamily = async (req: Request, res: Response) => {
    try {
        // Lógica para criar uma família
    } catch (error) {
        console.error(error);
    }
}

export const editFamily = async (req: Request, res: Response) => {
    try {
        // Lógica para editar uma família
    } catch (error) {
        console.error(error);
    }
}

export const deleteFamily = async (req: Request, res: Response) => {
    try {
        // Lógica para deletar uma família
    } catch (error) {
        console.error(error);
    }
}